name: build-libvips
env:
  libvipsVersion: '8.10.5'

#readonly LIBVPS_VERSION=8.10.5
#readonly SRC=https://github.com/libvips/libvips/releases/download/v${LIBVPS_VERSION}/vips-${LIBVPS_VERSION}.tar.gz
#readonly DEST=td-libvips-dev_${LIBVPS_VERSION}
#readonly DEB=${DEST}.deb

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
        run: ls -lah
#      - name: Install requirements
#        run: |
#          sudo apt-get install -y --no-install-recommends \
#            apt-utils \
#            automake
#            build-essential \
#            ca-certificates \
#            cmake \
#            curl \
#            debconf \
#            debhelper \
#            dpkg \
#            git \
#            gnupg \
#            gobject-introspection \
#            libexpat1-dev \
#            libglib2.0-dev \
#            pkg-config \
#            software-properties-common \
#            wget
#      - run: ls -lah
#        working-directory: tada-team.gitpages.io/libvpis
#      - name: Make deb template
#        working-directory: ./libvpis
#        run: |
#          pwd
#          ls -lah
#          cp -rv DEBIAN.template td-libvips-dev_${{env.libvipsVersion}}/DEBIAN
#          sed -i "s/%LIBVPS_VERSION%/${{env.libvipsVersion}}/g" td-libvips-dev_${{env.libvipsVersion}}/DEBIAN/*
